import React from 'react';
import './WeatherDetailLabel.css';

const WeatherDetailLabel = ({ children }) => {
  return (
    <div className="WeatherDetailLabel">{children}</div>
  );
};

export default WeatherDetailLabel;
